{
  "name": "anima",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "lint": "yarn standard",
    "test": "jest"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "./**/*.{js}": [
      "yarn lint --fix",
      "jest --bail --coverage --findRelatedTests"
    ]
  },
  "dependencies": {},
  "devDependencies": {
    "husky": "^3.0.8",
    "jest": "^24.9.0",
    "lint-staged": "^9.4.1",
    "standard": "^14.3.1"
  }
}
