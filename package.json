{
  "name": "anima",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "lint": "yarn standard --globals expect",
    "fix": "yarn lint --fix",
    "test": "jest --coverage ",
    "fixAndTest": "yarn fix && yarn test"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged -c ./lint-staged.config.js"
    }
  },
  "dependencies": {},
  "devDependencies": {
    "documentation": "^12.1.4",
    "husky": "^3.0.8",
    "jest": "^24.9.0",
    "jest-cucumber": "^2.0.11",
    "lint-staged": "^9.4.1",
    "prettier": "^1.18.2",
    "standard": "^14.3.1"
  }
}
